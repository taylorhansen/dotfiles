#!/usr/bin/env bash

# shortened git command
alias g=git
# make sure normal git autocomplete works with it
source /usr/share/bash-completion/completions/git
__git_complete g __git_main

# shortened npm command
alias nr='npm run'

# shortened radian command
alias r=radian
